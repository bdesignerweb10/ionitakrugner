<?php
	/*$id = $_GET['id'];
	$jurisprudencia = $conn->query("select * from tbl_jurisprudencia where id_jurisprudencia = $id") or trigger_error($conn->error);
	if ($jurisprudencia && $jurisprudencia->num_rows > 0) {						
			while($juris = $jurisprudencia->fetch_object()) {
				$descricao = $juris->descricao;
		}
	}*/
	
?>